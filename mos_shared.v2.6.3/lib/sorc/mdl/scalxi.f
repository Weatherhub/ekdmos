      SUBROUTINE SCALXI(KFILDO,XDATA,NVAL,CONST,NSCALE,IER)
C
c        MARCH     2007   GLAHN   MDL   MOS-2000
C                                 ADAPTED FROM SCALX
C        JUNE      2008   GLAHN   ADDED DECIMAL POINT 10.**NSCALE
C
C        PURPOSE
C            TO SCALE A VARIABLE X CONST*10.**NSCALE ROUNDED TO THE 
C            NEAREST WHOLE VALUE.  THIS CAN BE USED FOR VECTOR OR
C            GRIDDED DATA.
C   
C        DATA SET USE
C            KFILDO    - UNIT NUMBER OF OUTPUT (PRINT) FILE.  (OUTPUT)
C
C        VARIABLES
C              KFILDO = UNIT NUMBER OF OUTPUT (PRINT) FILE.  (INPUT)
C            XDATA(K) = THE DATA TO SCALE (K=1,NVAL).  (INPUT-OUTPUT)
C               CONST = THE MULTIPLIER FOR SCALING.  (INPUT)
C              NSCALE = THE POWER OF TEN FOR SCALING.  (INPUT)
C                NVAL = THE NUMBER OF VALUES IN XDATA( ) BEING DEALT
C                       WITH.  (INPUT)
C                 IER = ERROR RETURN.
C                       0 = GOOD RETURN.
C                       (OUTPUT)
C        1         2         3         4         5         6         7 X
C 
C        NONSYSTEM SUBROUTINES USED 
C            NONE
C
      DIMENSION XDATA(NVAL)
C
      IER=0
C
      FACTOR=CONST*10.**NSCALE
C
D     WRITE(KFILDO,100)NVAL,NSCALE,CONST,FACTOR
D100  FORMAT(/' IN SCALX AT 100--NVAL,NSCALE,CONST,FACTOR',2I8,2F8.2)
C
      DO 120 K=1,NVAL
      IF(XDATA(K).NE.9999.)XDATA(K)=NINT(XDATA(K)*FACTOR)
 120  CONTINUE
C
D     WRITE(KFILDO,125)(XDATA(K),K=1,NVAL)
D125  FORMAT(/,' IN SCALXI',/,(15F8.2))
C
      RETURN
      END
